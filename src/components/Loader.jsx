//Loader animacija iz antdesign Spin komponente

import React from "react";
import { Spin } from "antd";

const Loader = () => (
  <div className="loader">
    <Spin />
  </div>
);

export default Loader;
